name: Build & Release DXT

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build (loader + bundle)
        run: npm run build

      - name: Install DXT CLI
        run: npm install -g @anthropic-ai/dxt

      - name: Pack DXT
        run: dxt pack

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "MCP Jira ${{ github.ref_name }}"
          body: |
            ## MCP Jira v${{ steps.version.outputs.VERSION }}

            ### 🔄 Auto-update enabled
            Nếu đã cài v1.0.5+ trước đó, **chỉ cần restart Claude Desktop** (⌘+Q rồi mở lại) — bundle mới sẽ tự động được tải về. Không cần tải lại file `.dxt`.

            ### 📦 Cài lần đầu
            1. Tải file `mcp-jira.dxt` bên dưới
            2. Mở Claude Desktop → Settings → Extensions
            3. Kéo thả file `.dxt` vào → Install
            4. Nhập username/password Jira → Save

            ### 📚 Hướng dẫn sử dụng
            Xem [README.md](https://github.com/${{ github.repository }}/blob/main/README.md) và [prompts.md](https://github.com/${{ github.repository }}/blob/main/prompts.md)
          files: |
            mcp-jira.dxt
            dist/bundle.js
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
